# --- Security (CRITICAL) ---
.env
# This file contains your PI_SECRET. Never commit it!

# --- Python & Virtual Environments ---
__pycache__/
*.pyc
venv/
env/
.venv/
.pytest_cache/

# --- IDE Settings ---
.vscode/
.idea/
.DS_Store

# --- Runtime & Debug Files ---
# We don't want to upload the temporary photos the camera takes
static/detected.jpg
static/*.jpg
static/*.png

# --- System & Logs ---
*.log
*.sqlite3

# --- Optional: Keep the model, but ignore heavy training data ---
# We generally WANT the .tflite model in git so others can run it.
# If you have large datasets, ignore them:
# dataset/